version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - frontend
      - backend

  frontend:
    build:
      context: ./client
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost/api
      - VITE_SOCKET_URL=http://localhost

  backend:
    build:
      context: ./server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://inderash18_db_user:mongodb10@cluster0.zrs1ebj.mongodb.net/humanhub?retryWrites=true&w=majority
      - PG_URI=postgresql://postgres:postgres@postgres:5432/humanhub
      - REDIS_URL=redis://redis:6379
      - FRONTEND_URL=http://localhost
      - AI_SERVICE_URL=http://ai_services:8000
    depends_on:
      - mongo
      - postgres
      - redis

  ai_services:
    build:
      context: ./ai_services
    ports:
      - "8000:8000"
    volumes:
      - ./ai_services:/app
    environment:
      - MODEL_CACHE_DIR=/models

  postgres:
    image: postgres:14-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=humanhub
    volumes:
      - pg_data:/var/lib/postgresql/data

  mongo:
    image: mongo:6
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db


  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

volumes:
  pg_data:
  mongo_data:
